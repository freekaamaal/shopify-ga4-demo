(function(){
  try{
    // Intentionally blank — we decided not to override.
    // You could add lightweight logging here later if you host a simple endpoint.
  }catch(e){ try{console.warn('stage2-keep error',e);}catch(_){ } }
})();
